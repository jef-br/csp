/target
**/*.rs.bk
Cargo.lock.orig

# Local editor + local-only reference material (not part of the project)
.vscode/
MD/
.serena/
graphify-out/

# Model + ONNX Runtime: build inputs, compiled into the exe, never in the repo
# (see docs/ARCHITECTURE.md 5). Every .onnx lives in models/ - see models/README.md.
*.onnx
*.onnx.data
onnxruntime.dll
onnxruntime_providers_shared.dll

# Local run output
/shot-classifier-output
/test data/OUTPUT

# Python bytecode from tools/
__pycache__/
*.pyc
